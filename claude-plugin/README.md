# avad claude plugin surface

Generated by `avad-core/scripts/materialize-plugin.ts`.

Do not edit by hand. Edit the canonical sources under `avad-core/`
(`hooks/`, `bootstrap/`, `shared/`, `skills/`) and rerun:

```
bun avad-core/scripts/materialize-plugin.ts
```

CI fails if the committed surface disagrees with a fresh rerun.
